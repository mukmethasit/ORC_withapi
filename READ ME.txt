เครื่องมือนี้คือ Jupyter Notebook ที่ใช้สำหรับการทำ OCR (Optical Character Recognition) บนรูปภาพ โดยใช้ Google Gemini API เป็นเครื่องมือหลักในการแปลงรูปภาพเป็นข้อความ และมีอินเทอร์เฟซผู้ใช้แบบโต้ตอบ (UI) ที่สร้างด้วย ipywidgets

🚀 คุณสมบัติหลัก
Cloud-Powered OCR: ใช้โมเดล Gemini 2.0 Flash ซึ่งมีความสามารถด้าน Multimodal ที่แม่นยำสูงในการอ่านข้อความและทำความเข้าใจโครงสร้างเอกสาร

Prompt Engineering: ใช้ Prompt ที่ออกแบบมาเฉพาะทาง (OCR Expert) เพื่อให้ได้ผลลัพธ์ที่เป็นข้อความ (Text) และรักษา Formatting (Markdown) ได้อย่างดีเยี่ยม

Manual Crop/ROI Selection: มีฟังก์ชันให้ผู้ใช้สามารถเลือก Crop เฉพาะพื้นที่ที่ต้องการประมวลผล (Region of Interest - ROI) ก่อนส่งรูปภาพไปยัง API (ต้องติดตั้ง OpenCV บนเครื่องหรือสภาพแวดล้อมที่รัน)

Interactive UI: ใช้งานง่ายผ่าน Dashboard ที่สร้างด้วย ipywidgets เหมาะสำหรับรันบน Jupyter/Colab/Voila

⚙️ ข้อกำหนดเบื้องต้นและการตั้งค่า API Key
เนื่องจาก Notebook นี้เรียกใช้ Gemini API จึงจำเป็นต้องตั้งค่า API Key ของคุณ:

รับ API Key: หากคุณยังไม่มี ให้สร้าง API Key ที่ Google AI Studio

การตั้งค่าในโค้ด (Cell 1): เพื่อความปลอดภัย คุณควรเก็บ API Key เป็นความลับ และหลีกเลี่ยงการ Hardcode

วิธีที่แนะนำ (ใช้ Environment Variable): แก้ไข Cell 1 ดังนี้:

Python

import os
# แทนที่ด้วยชื่อ Environment Variable ที่คุณตั้งค่าไว้
GOOGLE_API_KEY = os.getenv("GEMINI_API_KEY") 

# ตรวจสอบ:
if not GOOGLE_API_KEY:
    print("❌ Error: GEMINI_API_KEY environment variable not set.")
    # สามารถใช้ ipywidgets.Password เพื่อให้ผู้ใช้กรอกขณะรันได้
วิธีที่ง่ายกว่า (สำหรับทดสอบส่วนตัว): หากรันใน Colab หรือ Jupyter ส่วนตัว คุณสามารถแทนที่ในโค้ดได้โดยตรง (แต่ไม่แนะนำให้แชร์โค้ดที่มี Key):

Python

# Cell 1: แทนที่ข้อความ YOUR_API_KEY_HERE ด้วยคีย์จริงของคุณ
GOOGLE_API_KEY = "YOUR_API_KEY_HERE" 
   วิธีใช้งานด้วย Voila (Recommended)
Voila ช่วยเปลี่ยน Jupyter Notebook ที่มี ipywidgets เป็น Web Application ที่ใช้งานง่าย:

ติดตั้ง Voila: หากยังไม่ได้ติดตั้ง

Bash

pip install voila
รัน Voila:

Bash

voila orcwithapi.ipynb
ใช้งาน: Voila จะเปิดหน้า Dashboard (Cell 5) ในเว็บเบราว์เซอร์ ซึ่งคุณสามารถ:

เลือกไฟล์รูปภาพที่อยู่ในไดเรกทอรีเดียวกัน

ใส่ Topic ที่ต้องการให้ Gemini เน้น

เลือก Manual Crop (หากรัน Voila ในสภาพแวดล้อมที่รองรับ cv2.selectROI ซึ่งอาจไม่รองรับในบาง Cloud Host)

คลิก -> Run OCR เพื่อรับผลลัพธ์และบันทึกในไฟล์ gemini_ocr_output.txt

   ลิงก์ที่เกี่ยวข้อง
Voila Documentation: https://voila.readthedocs.io/en/stable/

Gemini API Key: https://ai.google.dev/gemini-api/docs/api-key